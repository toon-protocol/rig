---
---

chore(factory): stand up the sandcastle software factory (`.sandcastle/` scaffold, pnpm Dockerfile, `agent:implement`/`agent:review` runners, `typecheck` script). Tooling only — no runtime change to `@toon-protocol/rig`, so no version bump.
